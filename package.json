{
  "name": "campusbites",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd campus-bites && npm install --include=dev && npm run build",
    "vercel-build": "cd campus-bites && npm install --include=dev && npm run build"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.4.1",
    "google-auth-library": "^10.5.0",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.0.0",
    "nodemailer": "^7.0.12",
    "razorpay": "^2.9.6",
    "firebase-admin": "^13.0.0"
  }
}
