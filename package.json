{
  "name": "campusbites",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd campus-bites && npm install && npm run build",
    "vercel-build": "cd campus-bites && npm install && npm run build"
  }
}
